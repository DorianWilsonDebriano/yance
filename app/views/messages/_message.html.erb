<div class="main-messages-container">
  <% if message.user == current_user %>
    <div class="message-container text-right current-user-message-container" id="message-<%= message.id %>">
      <i class="author current-user-author">
        <small><%= local_time(message.created_at.strftime("%a %b %e at %l:%M%p")) %></small>
        <span><%= message.user.first_name %></span>
        <span><%= message.user.last_name %></span>
        <% if message.user.photo.attached? %>
          <%= cl_image_tag message.user.photo.key, class: "avatar dropdown-toggle user-avatar", id: "user-chatroom-avatar" %>
        <% end %>
      </i>
      <div class="message-paragraph current-user-messenger">
        <p><%= message.content %></p>
      </div>
    </div>
  <% else %>
    <div class="sports-class-message-container text-left not-current-user-message-container" id="message-<%= message.id %>">
      <i class="author">
        <% if message.user.photo.attached? %>
          <%= cl_image_tag message.user.photo.key, class: "avatar dropdown-toggle user-avatar" %>
        <% end %>
        <span><%= message.user.first_name %></span>
        <span><%= message.user.last_name %></span>
        <small>sent: <%= local_time(message.created_at.strftime("%a %b %e at %l:%M%p")) %></small>
      </i>
      <div class="message-paragraph">
        <p><%= message.content %></p>
      </div>
    </div>
  <% end %>
</div>
